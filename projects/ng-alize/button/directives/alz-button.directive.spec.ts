import { AlzButtonDirective } from './alz-button.directive';

describe('AlzButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AlzButtonDirective();
    expect(directive).toBeTruthy();
  });
});
