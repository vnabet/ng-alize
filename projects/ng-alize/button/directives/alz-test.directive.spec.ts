import { AlzTestDirective } from './alz-test.directive';

describe('AlzTestDirective', () => {
  it('should create an instance', () => {
    const directive = new AlzTestDirective();
    expect(directive).toBeTruthy();
  });
});
