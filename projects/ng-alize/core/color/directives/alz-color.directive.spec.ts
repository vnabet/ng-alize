import { AlzColorDirective } from './alz-color.directive';

describe('AlzColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AlzColorDirective();
    expect(directive).toBeTruthy();
  });
});
